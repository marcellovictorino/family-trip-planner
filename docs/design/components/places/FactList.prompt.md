One-line: the muted-term / plain-value fact grid inside an expanded place card (Metro, Best time, Booking).

```jsx
<FactList items={[["Metro", "Nørreport"], ["Best time", "morning"], ["Booking", "none"]]} />
```

Facts only — anything that needs a sentence belongs in the description or a Tip line.
