One-line: small counted facts on the Trip tab, always in a two-column `StatGrid`.

```jsx
<StatGrid>
  <Stat value="12/85" label="visited" />
  <Stat value={7} label="favourites" />
</StatGrid>
```

Counts only — no percentages, no progress bars. The product deliberately has no completion score.
