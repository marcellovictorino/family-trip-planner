One-line: the coloured category pill that stands in for a hero image on every place card.

```jsx
<KindBand kind="playground" label="playground" />
```

Three kinds only — attraction (blue), playground (green), restaurant (sand). Colours are fixed per kind; never recolour a band for emphasis, and never add a fourth kind without adding it to the data schema first.
