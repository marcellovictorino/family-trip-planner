One-line: the one search input in Explore — there is never a second search box or a field selector.

```jsx
<SearchField value={query} onChange={(e) => setQuery(e.target.value)} />
```

Full width, 44px minimum height, 10px radius. Placeholder names the fields it covers rather than saying "Search…".
