One-line: the heading above each day, saved group or trip section, with counts as muted meta.

```jsx
<SectionHeading meta="3 stops · 4h30">Monday, 3 August</SectionHeading>
```

Display face, semibold, small — headings stay quiet so place names read as the loudest text on screen.
